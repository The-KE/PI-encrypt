name: Build Unix program

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: build
      run: gcc main.c -o piEncrypt
    - uses: actions/upload-artifact@v4
      with:
          name: mhm
          path: piEncrypt
